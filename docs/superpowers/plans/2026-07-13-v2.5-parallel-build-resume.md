# v2.5 parallel build — PAUSED, resume doc

Paused 2026-07-13 mid-Phase-2 at the user's request. This doc is everything a fresh
session needs to resume. Delete it (or fold into the release notes) when v2.5.0 ships.

## Goal

Five setup-experience improvements, built by parallel subagents, adversarially reviewed,
shipped as v2.5.0: (1) plugin-native hooks ✅ done, (2+3) /doctor skill with engine
bootstrap + bundled MCP definition, (5) portability, (4) docs overhaul.

## Live dashboard

https://claude.ai/code/artifact/3b9c1bf0-46be-49d9-8181-8bd7b1271428
Update by editing the session scratchpad copy and republishing, or from a new session pass
this URL as the `url` param of the Artifact tool. Statuses to flip on resume: Phase 2
"paused" → "running", WS cards accordingly.

## Git state (all local, NOTHING pushed since v2.4.1 @ 15086a4)

- `main` = `77c325c` — WS1 merged: plugin-native hooks. Suite: **123/123** (`./test.sh`).
- Worktrees (siblings of the repo, all forked from 77c325c):
  - `/Users/abhi/Desktop/headroom-plugin-ws2-doctor` → branch `feature/v2.5-ws2-doctor` — **clean, no work started** (agent was killed while orienting).
  - `/Users/abhi/Desktop/headroom-plugin-ws3-portability` → `feature/v2.5-ws3-portability` — **uncommitted partial work** in `bin/hcat`, `scripts/dangi-hook.sh`, `test.sh` (agent was mid-TDD: new tests written, killed right before running the RED pass — treat the diff as tests-plus-possible-partial-impl, verify RED/GREEN state before continuing).
  - `/Users/abhi/Desktop/headroom-plugin-ws4-docs` → `feature/v2.5-ws4-docs` — **uncommitted partial work** in `README.md`, `skills/headroom-usage-indicator/SKILL.md` (agent had finished quickstart/verify sections, was killed while "restructuring the Install section — statusLine-only installer for plugin installs, full installer demoted to legacy fallback").

## Phase-0 research facts (from claude-code-guide agent — trust these)

- `hooks/hooks.json` at plugin root auto-registers hooks on plugin enable; full parity with
  settings.json hooks (PreToolUse `permissionDecision: deny`, PostToolUse
  `additionalContext`); `${CLAUDE_PLUGIN_ROOT}` resolves in command strings.
- `bin/` at plugin root joins Claude's Bash PATH while the plugin is enabled (hence
  `bin/hcat`, already shipped in WS1).
- Plugins can bundle MCP servers via `.mcp.json` at plugin root — auto-starts on enable,
  tools appear as `mcp__plugin_<plugin>_<server>__<tool>`. This lets us erase the manual
  "register headroom MCP" step (WS2 scope).
- statusLine can NOT be reliably plugin-provided (plugin settings.json merge is
  undocumented/unguaranteed) — keep it a settings.json entry that the doctor writes.
- Version-gated: hooks auto-registration + MCP bundling stable v2.1+; bin-on-PATH and
  plugin-settings merge less documented. Cache path is per-version:
  `~/.claude/plugins/cache/{plugin}/{version}/`. Updates via `/plugin update` after
  bumping version in `.claude-plugin/plugin.json` + `marketplace.json`.

## WS1 (DONE, merged @ 77c325c)

hooks/hooks.json (PreToolUse `Read|Bash` → gate, PostToolUse `*` → dangi, via
`"${CLAUDE_PLUGIN_ROOT}"/scripts/...`); `git mv scripts/hcat bin/hcat`; nudge/deny text now
says plain `hcat "<path>"` (on PATH); gate probes `../bin/hcat` then legacy sibling; SKILL.md
installer path-patched. 24 new checks incl. end-to-end through the literal hooks.json command
strings. Carry-forward concerns for other workstreams:
- Legacy installs (copies in ~/.claude + settings.json hooks) double-fire alongside
  plugin-native hooks → WS2 doctor must detect + migrate.
- "on PATH" wording is only true for plugin installs → WS4 legacy appendix must say so.

## WS2 spec (restart fresh in its worktree — nothing to salvage)

Owns: `scripts/doctor.sh`, `skills/doctor/SKILL.md`, `.mcp.json`, `scripts/mcp-launcher.sh`,
one new test.sh section. Must NOT touch README, other SKILL.md, dangi/gate/statusline/bin.
1. `doctor.sh` — read-only checks, aligned `ok -`/`FAIL -`/`fixable -`/`skip -` lines, exit
   0 iff no FAILs: jq; engine python (resolution order: $HCAT_PYTHON → sibling `python` of
   `headroom` on PATH → `~/.headroom-venv/bin/python`) + `import headroom`; bin/hcat
   executable + real smoke compression of a generated ~20KB JSON (skip when engine absent);
   hooks/hooks.json parses with both events; legacy dual-registration in settings.json
   (hook commands mentioning ~/.claude dangi-hook.sh / hcat-gate.sh); statusLine wired;
   stale ~/.claude copies. Env overrides for hermetic tests: DOCTOR_SETTINGS,
   DOCTOR_CLAUDE_DIR, DOCTOR_VENV_DIR, HCAT_PYTHON.
2. `--fix`: venv bootstrap (python3 -m venv + pip install headroom; never run pip in
   tests — stub); legacy hook-entry removal via jq with timestamped .bak; statusLine write
   via jq merge ({"type":"command","command":"~/.claude/headroom-statusline.sh"}, copy the
   script there) with .bak; stale-copy cleanup only when plugin-native hooks confirmed.
   All fixes idempotent (test a double run).
3. `.mcp.json` + `scripts/mcp-launcher.sh`: launcher resolves engine (same order), execs
   the same server invocation currently registered on this machine — inspect read-only via
   `jq '.mcpServers.headroom // empty' ~/.claude.json` (fall back to other settings files);
   env HEADROOM_UPDATE_CHECK=off HF_HUB_OFFLINE=1; engine missing → one helpful stderr
   line naming the doctor + nonzero exit.
4. `skills/doctor/SKILL.md`: name `doctor` (invoked /<plugin-name>:doctor), triggers on
   headroom setup/doctor/not-working; run doctor.sh, interpret plainly, consent before --fix.
5. TDD in test.sh "32. doctor" — hermetic fixtures, every detector + fixer + launcher
   error path. Commit ONE commit on its branch:
   "feat: /doctor skill, engine bootstrap, bundled MCP definition (v2.5 WS2)".

## WS3 spec (resume in its worktree — inspect existing diff first, verify RED, continue TDD)

Owns: scripts/dangi-hook.sh, bin/hcat, scripts/statusline.sh, scripts/hcat-gate.sh
(portability only), one test.sh section.
1. Dangi Linux notifications: notify-send fallback when osascript absent; same
   NOTIFY_COOLDOWN + DANGI_NO_NOTIFY; fire-and-forget; TDD with fake notify-send on
   controlled PATH (mirror the fake-osascript test ~line 249).
2. bin/hcat: fix BrokenPipeError on `hcat big.json | head -1` inside the Python heredoc
   (SIGPIPE SIG_DFL, guarded); TDD: stderr must not contain "BrokenPipeError", stdout still
   starts with receipt header.
3. GNU/BSD sweep of stat/date/wc/sed/find/head -c in scripts/ and bin/ — repo pattern is
   try-one-then-other; fix gaps, report file:line findings.
Commit ONE commit: "fix: linux notify-send fallback, hcat SIGPIPE, GNU/BSD sweep (v2.5 WS3)".

## WS4 spec (resume in its worktree — inspect existing diff first, continue)

Owns README.md + skills/headroom-usage-indicator/SKILL.md ONLY. Killed mid-"Install section
restructure". Full task list: quickstart top (2 commands + doctor); copy-paste "verify it
works" block (hcat a big JSON → `── hcat:` receipt, badge flips green; run the doctor);
Uninstall section; GIF placeholder (HTML comment, no fabricated image); legacy manual
install → appendix with accurate PATH wording; "Updating" note (/plugin update vs legacy
re-run installer). SKILL.md: installer prose shrinks to legacy migration + statusLine setup
+ manual fallback; keep frontmatter + functional steps. Commands must be copy-pasteable
against the real layout. Commit ONE commit:
"docs: quickstart, verify block, uninstall, legacy appendix (v2.5 WS4)".

## Phase 3 (not started) — integrate · review · ship

1. Merge order into main: WS2 → WS3 → WS4 (expect a test.sh append conflict between
   WS2/WS3 — both add a numbered section at the end; renumber, keep both).
2. Full suite after each merge; final target ALL green (123 + WS2 + WS3 additions).
3. Adversarial review panel (parallel subagents, must-refute prompts): correctness lens,
   shell-portability lens, docs-accuracy lens (docs claims vs actual file layout/behavior).
   Fix confirmed findings TDD-style.
4. Thorough live testing on this machine: statusline render against real transcript;
   doctor run (real, no --fix, then --fix dry assessment); hcat smoke; hooks end-to-end.
5. Bump `.claude-plugin/plugin.json` + `marketplace.json` to 2.5.0; README release note.
6. Refresh installed legacy copies in ~/.claude (this machine is a legacy install until
   the plugin-native flow is verified here).
7. Ship: feature-branch commits already exist → merge main, tag v2.5.0, push main+tag,
   `gh release create` (needs explicit user OK — classifier blocks otherwise).
8. Update memory (headroom-plugin-v2.md + MEMORY.md index) and the dashboard to "shipped".

## Decisions already made

- WS2+WS3 own their own test.sh sections; integration resolves the append conflict.
- statusLine stays settings-side (doctor writes it) — plugin merge unreliable.
- Doctor's fixes ARE the engine bootstrap (#2 and #3 were merged into WS2).
- Agents work in worktrees, ONE commit each, orchestrator merges --no-ff and reviews.
- Commit trailer: "Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>".
